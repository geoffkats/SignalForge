[pytest]
# importlib mode avoids the 'tests' package namespace collision when running
# ml/tests/ and backend/tests/ from the repo root in a single pytest invocation.
addopts = --import-mode=importlib
